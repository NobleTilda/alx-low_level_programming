#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first number to multiply
 * @b: second to be multiplied with the first number
 * Return: the result of the multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
